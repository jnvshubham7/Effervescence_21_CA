<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.effe_21ca">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Effe21CA">
        <activity android:name=".twelve_page"></activity>
        <activity android:name=".eleven_page" />
        <activity android:name=".ten_page" />
        <activity android:name=".nine_page" />
        <activity android:name=".eight_page" />
        <activity android:name=".MainActivity" />

        <activity android:name=".seven_page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>